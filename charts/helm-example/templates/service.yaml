apiVersion: v1
kind: Service
metadata:
  name: {{ include "medium-deployment.fullname" . }}
  labels:
    {{- include "medium-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medium-deployment.selectorLabels" . | nindent 4 }}
