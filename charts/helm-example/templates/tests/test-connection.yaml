apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "medium-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "medium-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "medium-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
